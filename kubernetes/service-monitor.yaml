apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: monarch-db-metrics
  labels:
    app: monarch-db
spec:
  selector:
    matchLabels:
      app: monarch-db
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s

